import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.wilsonrosa.app',
  appName: 'LancerGroup-App',
  webDir: 'dist/app-ionic',
  bundledWebRuntime: false
};

export default config;
